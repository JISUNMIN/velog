📌 1. 고차 함수 (Higher-Order Function)
✅ 개념:
보통 함수는 숫자나 문자열 같은 값을 받고, 결과를 내보냄 ↔️
고차 함수는 함수도 값처럼 취급해서 함수를 인자로 받고 함수가 함수를 반환함
그래서 **함수 조작이 고차원적인 개념이라는 뜻에서 고차 함수**라 함
장점: 코드 재사용과 추상화가 편해짐

```
📦 예시 1: 함수를 인자로 받는 고차 함수
function repeat(n, action) {
  for (let i = 0; i < n; i++) {
    action(i);
  }
}

repeat(3, console.log);
// 출력: 0, 1, 2

📦 예시 2: 함수를 리턴하는 고차 함수
function greaterThan(n) {
  return function(m) {
    return m > n;
  };
}

const greaterThan10 = greaterThan(10);
console.log(greaterThan10(15)); // true
console.log(greaterThan10(5));  // false

🔁 map, filter, reduce도 고차 함수
```

📌 2. 커링 (Currying)
✅ 개념:
여러 개의 인자를 받는 함수를 하나의 인자씩 받도록 나누는 기법
f(a, b) ➜ f(a)(b) 형태로 바꾸는 것
장점: 코드 재사용(부분 적용)과 함수 조합이 편해짐

```
📦 예시 1: 일반 함수
function add(a, b) {
  return a + b;
}
console.log(add(2, 3)); // 5

📦 예시 2: 커링된 함수

function curriedAdd(a) {
  return function(b) {
    return a + b;
  };
}

console.log(curriedAdd(2)(3)); // 5

📦 예시 3: 화살표 함수로 커링
const curriedMultiply = a => b => a * b;

const double = curriedMultiply(2);
console.log(double(5)); // 10

```

#### 일반 함수 ↔️  커링 함수

일반 함수 - 부분 적용 불편함

🧩 **부분 적용(Partial Application)** 은 함수가 받을 인자 중 일부만 미리 채워서 새로운 함수를 만드는 걸 의미

```
function add(a, b) {
  return a + b;
}

const add5 = (b) => add(5, b);

console.log(add5(3)); // 8

```

**커링 함수 - 부분 적용 간편함**
```
예시1)
const add = a => b => a + b;

const add5 = add(5);  // 5가 고정된 새로운 함수 생성

console.log(add5(3)); // 8
console.log(add5(10)); // 15

add(5) 하면 즉시 5가 고정된 함수가 만들어져서 재사용 가능!

예시2)
const multiply = a => b => a * b;
const double = multiply(2);

console.log(double(5)); // 10
console.log(double(100)); // 200

console.log(multiply(3)(4)); // 12


```