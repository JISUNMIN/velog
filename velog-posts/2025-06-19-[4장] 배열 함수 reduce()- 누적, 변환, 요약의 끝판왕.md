`reduce()`는 배열 메서드 중 하나로 , 단순한 합꼐를 넘어서 배열을 원하는 형태로 **누적**, **변환**, **요약**할 수 있도록 도와줍니다.

---

## ✅ reduce()란?

`reduce()`는 배열의 각 요소를 순회하며 **이전 결과와 현재 값을 누적(accumulate)**해 나가는 함수입니다.  
말 그대로, 배열을 하나의 값으로 **축약(reduce)**합니다.

---

## 🧩 기본 문법

```js
arr.reduce(callback, initialValue);
```
- callback: 누적 로직을 정의하는 함수
- initialValue: 누적의 시작값 (생략 가능하지만 명시하는 걸 추천)

>🔹 callback 함수 형태
(prev, curr, index?, arr?) => { ... }

| 매개변수    | 설명             | 필수 여부          |
| ------- | -------------- |---------------------- |
| `prev`  | 이전 콜백 결과 (누적값) | 필수             |
| `curr`  | 현재 요소          | 필수             |
| `index` | 현재 인덱스         |선택적 (optional) |
| `arr`   | 원본 배열          |선택적 (optional) |


## 🛠️ 기본 예시

### 1. 숫자 누적합 구하기

```js
const numbers = [1, 2, 3, 4, 5];

const sum = numbers.reduce((acc, cur) => acc + cur, 0);
console.log(sum); // 15
```

### 2. 배열 요소 개수 세기
```js
const fruits = ['apple', 'banana', 'apple', 'orange', 'banana'];

const count = fruits.reduce((acc, fruit) => {
  acc[fruit] = (acc[fruit] || 0) + 1;
  return acc;
}, {});

console.log(count);
// { apple: 2, banana: 2, orange: 1 }

```
### 3. 중첩 배열 펼치기 (flat)
```js
const nested = [[1, 2], [3, 4], [5]];

const flat = nested.reduce((acc, cur) => acc.concat(cur), []);
console.log(flat); // [1, 2, 3, 4, 5]

```

### 💡 언제 reduce를 써야 할까?
> reduce()는 다른 배열 메서드로 표현하기 어려운 복합적인 로직이 필요할 때 유용합니다.

| 목적                 | 적합한 메서드      |
| ------------------ | ------------ |
| 단순 필터링             | `filter()`   |
| 조건에 맞는 하나 찾기       | `find()`     |
| 각각 변환              | `map()`      |
| 누적하거나, 형태를 바꿔야 할 때 | ✅ `reduce()` |

### 🚨 사용 시 주의할 점
- 초기값은 가능한 한 명시
초기값 없이 reduce를 쓰면 빈 배열일 때 에러가 날 수 있습니다.

예시) arr.reduct(callback)
 - 배열의 첫번째 요소가 초기값처럼 자동 사용되고
 - 두번쨰 요소부터 reduct 콜백이 실행됩니다.

```js
const arr = [10, 20, 30];
const sum = arr.reduce((acc, cur) => acc + cur);
// 초기값이 없으므로 acc = 10, cur = 20부터 시작
```

❗ 그런데 배열이 빈 배열이면?

 ```js
const empty = [];
empty.reduce((acc, cur) => acc + cur); // ❌ TypeError 발생
💥 TypeError: Reduce of empty array with no initial value
```

✅ 해결 방법
빈 배열에서도 안전하게 쓰려면 초기값을 지정:

```js
const empty = [];
const sum = empty.reduce((acc, cur) => acc + cur, 0); // ✅ 0 반환됨
```

- 불필요하게 복잡한 로직은 피해야 함
너무 많은 일을 reduce 하나로 하려 들면 가독성이 떨어질 수 있습니다.

## 💼 실무에서


**1. API 응답 데이터를 요약하거나 통계 처리할 때**  
  예를 들어 상품 목록을 받아와서 카테고리별 개수를 세는 작업은 `reduce()`로 간결하게 처리할 수 있습니다.  
  실무에서는 이처럼 서버 응답 데이터를 프론트에서 시각화나 요약을 위해 재가공하는 일이 매우 흔합니다.

  ```js
  const products = [
    { name: 'Keyboard', category: 'Electronics' },
    { name: 'Shoes', category: 'Fashion' },
    { name: 'Monitor', category: 'Electronics' },
  ];

  const countByCategory = products.reduce((acc, item) => {
    acc[item.category] = (acc[item.category] || 0) + 1;
    return acc;
  }, {});

  // 결과: { Electronics: 2, Fashion: 1 }
```

**2. 배열을 ID 기반의 객체로 변환할 때**

사용자 리스트 등을 빠르게 조회하기 위해 ID를 키로 하는 객체로 변환하는 경우가 많습니다.  
이는 상태 관리, 캐싱, 리렌더링 최적화 등에 자주 활용됩니다.

```js
const users = [
  { id: 1, name: 'Jane' },
  { id: 2, name: 'Tom' },
];

const userMap = users.reduce((acc, user) => {
  acc[user.id] = user;
  return acc;
}, {});

// 결과: { 1: {id:1, name:'Jane'}, 2: {…} }
```
**3. 체크박스나 선택값을 객체로 변환할 때**

여러 항목 중 선택된 값을 { [key]: true } 형태로 만들어야 할 때 자주 사용됩니다.
폼 처리, 필터 조건 구성, API 전송 등에 유용합니다.

```js
const selected = ['apple', 'banana'];

const result = selected.reduce((acc, key) => {
  acc[key] = true;
  return acc;
}, {});

// 결과: { apple: true, banana: true }

```

## ✅ 요약 정리

| 키워드     | 내용                                                   |
|------------|--------------------------------------------------------|
| 핵심 기능  | 누적, 변환, 요약                                       |
| 형태       | `arr.reduce(callback, initialValue)`                   |
| 주요 활용  | 합계 계산, 그룹화, 객체 생성, 배열 평탄화 등           |

