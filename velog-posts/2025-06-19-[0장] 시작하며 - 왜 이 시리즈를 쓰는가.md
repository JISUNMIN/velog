이 시리즈에서는 [MDN 문서](https://developer.mozilla.org/ko/)를 기반으로 브라우저에서 제공하는 주요 **Web API 객체와 함수**들을 정리할 예정입니다.  
기초적인 내용이지만, 실무에서 자주 사용되며 헷갈리기 쉬운 부분들을 중심으로 다룰 계획입니다.

웹 개발을 하면서,  
많은 개발자들이 `Blob`, `File`, `FormData` 같은 Web API 객체나  
`URL.createObjectURL()`, `fetch()` 같은 함수들을 정확히 구분하지 못한 채 사용하는 경우가 많습니다.

예를 들어 `Blob`과 `File`은 비슷해 보이지만 실제로는 다른 용도로 쓰이며,  
`fetch()`의 응답을 `.blob()`으로 처리하는 과정이나  
`URL.createObjectURL()`로 이미지 미리보기를 만드는 방법 등을  
깊이 이해하지 못한 채 습관적으로 쓰는 경우가 많습니다.

저 또한 개발을 하면서 차이가 무엇인지 궁금했고,
이러한 궁금증을 해소하고 정확하게 이해하며 활용하기 위해 이 시리즈를 작성하게 되었습니다.

## 첫 번째 주제: Blob vs File 객체 비교 및 활용법

그 첫 번째 주제로는  
웹 개발에서 자주 사용하는 Web API 객체 중 `Blob`과 `File`을 비교하며 시작합니다.

- 두 객체의 차이점과 쓰임새  
- 어떤 상황에서 어떤 객체를 써야 하는지  
- MDN 문서를 기반으로 한 설명 및 예제  
- 실무에서 이미지 업로드, 미리보기 등에 어떻게 활용하는지 사례 소개

이러한 부분을 명확히 정리해보려 합니다.  
웹 API에 대한 이해를 탄탄히 하고 싶은 분들께 도움이 되길 바랍니다. ✍️

---
