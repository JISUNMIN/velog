# HTTP의 한계와 HTTPS, 그리고 HTTP 프로토콜의 진화

## ⚠️ HTTP의 한계

HTTP는 정보를 누구나 볼수 있게 암호화하지 않은 상태로 전송하는 프로토콜입니다. 이처럼 암호화되지 않은 정보를 **평문(Plain Text)** 이라고 하는데, 평문은 개발자가 메시지를 쉽게 확인하고 작성할 수 있다는 점에서 장점이 있지만, 이 점을 악용하여 메시지를 **탈취**하거나 **개인정보를 유출**하는 보안 문제가 발생하기도 합니다.

### 🔓 대표적인 보안 위협: 중간자 공격(MITM, Man-In-The-Middle Attack)

**중간자 공격**이란 네트워크 통신 중 제3자가 끼어들어 데이터를 **도청**하거나 **조작**하는 기법입니다.

#### 예시:
> 회원가입 시 비밀번호 `abc123` 입력  
> → 중간자: "크큭..비밀번호는 abc123이군!"  
> → 서버

### ❌ 또 다른 문제: 통신 상대 확인 불가

HTTP는 요청자가 누구인지, 요청을 받는 서버가 신뢰할 수 있는지 판단할 방법이 없습니다.

#### 예시:
> 편의점에서 아르바이트를 하는데,  
> - 미성년자가 와도 술을 팔아야 함  
> - 보드게임용 지폐로 결제해도 받아야 함  

## 🔐 HTTPS란? (HyperText Transfer Protocol Secure)

보안 문제로 인한 피해를 줄이기 위해 등장한 프로토콜로, HTTP에 **`S`(Secure)** 가 추가된 형태입니다.  
HTTPS는 **메시지를 암호화하여** 제3자가 내용을 볼 수 없게 합니다.  

현재 인터넷 상위 100개 사이트 중 97개가 HTTPS를 사용합니다.

---

## 🔁 SSL과 TLS

HTTPS의 암호화 기능은 **SSL(Secure Sockets Layer)** 이 담당합니다.  
SSL은 클라이언트와 서버가 **데이터를 암호화**하여 안전하게 통신할 수 있도록 도와주는 보안 계층입니다.

OSI 계층 구조에서 보면, **HTTP 위에 SSL이 추가된 구조**입니다.

### 🔄 TLS와의 차이점

TLS(Transport Layer Security)는 SSL의 **후속 버전**입니다.  
SSL 3.0 이후부터는 이름이 TLS로 바뀌었고, 현재는 대부분 TLS를 사용하지만 **여전히 SSL이라는 이름이 널리 사용**되고 있습니다.

---

## 🔑 SSL의 암호화 방식

### 1. 🔐 대칭키 암호화 방식

- 하나의 키로 암호화와 복호화를 모두 수행
- 속도가 빠르지만 키를 안전하게 교환하기 어려움

#### 예시:
> 🧰 보물상자 하나, 열쇠 하나  
> 클라이언트와 서버가 같은 `1234` 키를 가지고 통신

---

### 2. 🔓  공개키 암호화 방식

- 서로 다른 두 개의 키 사용 (공개키, 개인키)
- 공개키로 암호화된 데이터는 개인키로만 복호화 가능

#### 예시:
> 🧰 보물상자를 빨간 열쇠로 잠갔다면, 파란 열쇠로만 열 수 있음  
> 클라이언트에게 공개키를 주고, 서버는 개인키로만 복호화 가능

---

### ✅ SSL에서는 두 가지 기법을 **함께 사용**하여 단점을 보완합니다.

---

## SSL의 동작 과정 (핸드셰이크 ~ 종료)

SSL은 TCP처럼 클라이언트와 서버 간에 통신 준비 과정을 먼저 진행합니다.

### 📡 SSL 3단계

1. **핸드셰이크 (HandShake)**  
   클라이언트와 서버가 서로 통신 준비 상태를 확인  
   → 서버는 공개키를 제공하고, 클라이언트는 대칭키를 암호화해서 전달

2. **세션(Session)**  
   대칭키를 기반으로 실제 데이터를 주고받음 (속도 빠름, 안전성 확보)

3. **세션 종료**  
   통신이 끝나면 세션 종료 메시지를 주고받고 연결을 종료

### ✅ SSL 동작 과정 요약 (1단계 ~ 6단계)
1. 클라이언트 인사(Client Hello)
- 클라이언트가 서버에 인사하고, 지원하는 암호화 방식, 랜덤 데이터 등을 전송함.

2. 서버 인사(Server Hello)
- 서버가 클라이언트에게 응답하며, 선택한 암호화 방식과 인증서(공개키 포함)를 전달.

3. 인증서 확인
- 클라이언트가 인증서를 CA(공인 인증 기관) 목록과 비교해 서버 신뢰 여부를 확인함.

4. 프리 마스터 키 생성
- 클라이언트가 세션 키 생성을 위한 임시 키(프리 마스터 시크릿)를 생성하고, 서버의 공개키로 암호화하여 전송.
5. 세션 키 생성
- 서버는 받은 프리 마스터 키를 자신의 비밀키로 복호화하여 클라이언트와 동일한 세션 키 생성.

6. 암호화 통신 시작
- 세션 키로 데이터 암호화 통신을 시작하고, 통신 종료 시 세션 키는 삭제됨.



---

## 📈 HTTP의 변천사

### HTTP/0.9  
- HTML 문서만 전송 가능  
- 헤더 없음, GET 메서드만 지원  

### HTTP/1.0  
- 헤더 추가  
- `GET`, `HEAD`, `POST` 지원  

### HTTP/1.1 (1997, 표준 버전)  
- **지속적 연결 (Persistent Connection)**  
  - TCP 연결을 한 번 맺으면 계속 유지  
- **파이프라이닝 (Pipelining)**  
  - 이전 응답이 끝나기 전에 다음 요청을 보낼 수 있음

#### 예시 (레스토랑 비유):
> HTTP/1.0:  
> 와인을 얻기 위해 문을 열고 주문 → 받은 후 문을 닫음  
> 피자를 얻기 위해 다시 문을 열고 주문  

> HTTP/1.1:  
> 문을 열고 와인과 피자를 각각 주문

---

## HTTP/2 (2009)  
- 성능 개선  
- **스트림 다중화 (Multiplexed Streams)**  
  - 하나의 연결에서 여러 메시지를 동시 전송  
  - 응답 순서와 관계없이 처리 가능

#### 예시:
> 문을 열고 와인·피자 세트를 동시에 주문

---

## HTTP/3 (2020)  
- HTTP/2의 한계점인 TCP 기반 문제 해결  
- **TCP의 HOLB(Head-of-Line Blocking)** 문제 회피  

### ✅ 해결책: QUIC(Quick UDP Internet Connection)

- **UDP 기반 전송 프로토콜**  
- 멀티플렉싱 기술 활용  
- 하나의 경로에 문제가 생겨도 다른 경로로 전송 가능 → 병목 현상 완화

---

## ✅ 요약 정리

| 프로토콜        | 특징            | 한계/개선점           |
| ----------- | ------------- | ---------------- |
| 🔓 HTTP     | 평문 전송         | 보안 취약 (MITM 등)   |
| 🔐 HTTPS    | SSL 기반 암호화    | HTTP의 보안 문제 해결   |
| ⚙️ HTTP/1.1 | [표준 버젼] 지속 연결, 파이프라이닝 | 느린 전송 속도         |
| 🚀 HTTP/2   | 스트림 다중화       | TCP 기반으로 인한 HOLB |
| 🛸 HTTP/3   | QUIC(UDP 기반)  | HOLB 문제 해결       |

