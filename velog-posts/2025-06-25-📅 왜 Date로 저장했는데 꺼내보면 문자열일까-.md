> Prisma에서 `new Date()`로 저장한 값이 왜 `string`으로 나올까?

---

## 🔍 상황 설명

전 장 seed.ts 파일로 스크립트를 실행했습니다. 그리고 Prisma로 날짜 데이터를 이렇게 저장했습니다.

```ts
deadline: new Date("2025-12-31")
```

DB에도 timestamp 타입으로 잘 저장됐고, 문제없어 보였습니다.
하지만 값을 꺼내서 콘솔에 찍어보니, 타입이 문자열(string)로 나왔고,
Date 객체인지 확인하는 instanceof Date 결과는 false였습니다.

```ts
console.log(typeof project.deadline); // string
console.log(project.deadline instanceof Date); // false
```

## ❓  왜 이런 현상이 발생할까?
Prisma는 서버에서 데이터베이스의 `DateTime` 필드를 **JavaScript의 `Date` 객체**로 반환합니다.  
그래서 서버 내부에서 `console.log(project.deadline instanceof Date)`를 하면 `true`가 나옵니다.

하지만, 서버가 클라이언트에 데이터를 보낼 때는 보통 **JSON 포맷으로 직렬화**해서 전달하는데,  
이 과정에서 `Date` 객체는 자동으로 **ISO 8601 문자열** (예: `"2025-12-31T00:00:00.000Z"`)로 변환됩니다.

이 직렬화 과정은 `JSON.stringify()`나 Next.js의 `NextResponse.json()` 같은 함수가 수행합니다.

즉,  
- Prisma는 서버 내에서 `Date` 객체를 반환하고,  
- 서버가 클라이언트에 응답할 때는 JSON 직렬화 과정에서 `Date` 객체가 문자열로 변환되어 전송되고,  
- 클라이언트에서는 이 날짜 문자열을 받아서 필요하면 다시 `new Date()`로 감싸서 사용해야 합니다.

## 🛠️ 해결 방법
클라이언트에서 받은 문자열을 다시 Date 객체로 변환해서 사용하면 됩니다.

```ts
const deadline = new Date(project.deadline);

console.log(deadline instanceof Date); // true
console.log(deadline.getFullYear()); // 2025
```

## ✅ 요약 정리

| 저장 시                     | 불러올 때 (Prisma) | API 응답 / 클라이언트에서 | 타입       |
| ------------------------ | -------------- | ---------------- | -------- |
| `new Date("2025-12-31")` | `Date` 객체      | ISO 8601 문자열     | `string` |


**Prisma는 날짜를 저장할 때 `Date` 객체를 사용하고,
가져올 때도`Date` 객체로 반환하지만,
JSON으로 응답할 때는 문자열로 변환되어 클라이언트에서 받습니다.**
=> NextResponse.json의`.json()` 때문에 `Date`가 문자열로 바뀌게 됩니다.


## 💡구현

제가 실제로 적용한 날짜 포맷 함수와 사용 예시는 다음과 같습니다.

```ts

// .helper.ts
export const convertDateToString = (date: Date, separator = ""): string => {
  if (!date || !(date instanceof Date)) {
    console.error("Invalid Date");
    return ""; // 빈 문자열 반환
  }
  const year = date.getFullYear();
  const month = (date.getMonth() + 1).toString().padStart(2, "0");
  const day = date.getDate().toString().padStart(2, "0");

  switch (separator) {
    case "ko":
      return `${year}년 ${month}월 ${day}일`;
    default:
      return `${year}${separator}${month}${separator}${day}`;
  }
};
```
```tsx
<p className="text-gray-600">
  🗓 마감일: {convertDateToString(new Date(project.deadline), '-')}
</p>
```

이렇게 new Date()로 문자열을 다시 감싸서 Date 객체로 만든 후 포맷 함수에 전달하면,
원하는 형식으로 날짜를 표시할 수 있습니다.


