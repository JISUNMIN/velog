🧩 ts-node는 TypeScript 파일을 바로 실행할 수 있도록 해주는 Node.js 실행기
컴파일(.ts → .js) 없이 곧바로 실행 가능

🚀 실행 방법
```
package.json에 "type": "module" 추가

npx ts-node [경로명]
ex) 1. CommonJS일 경우 : npx ts-node src/index.ts
    2. ESM일 경우:
       - npx ts-node --loader ts-node/esm src/index.ts
       - node --loader ts-node/esm src/index.ts