> 이번 글에서는 리액트 프로젝트에서 자주 쓰는 `useTranslation` 훅,
그 내부 동작과 설정 방법을 정리하면서,
함께 react-i18next의 기본 구조도 살펴보겠습니다.

---

## 🌍 i18n, i18next, react-i18next의 관계

 

| 이름 | 의미 | 역할 |
|------|------|------|
| **i18n** | *internationalization(국제화)*의 약자 | 여러 언어를 지원하는 시스템 전반을 의미 |
| **i18next** | i18n을 쉽게 구현하도록 만든 자바스크립트 라이브러리 | 번역 리소스 관리, 언어 전환 등 핵심 엔진 역할 |
| **react-i18next** | i18next를 React에서 쉽게 쓸 수 있게 만든 바인딩 라이브러리 | `useTranslation` 훅 등 React 전용 API 제공 |

> **i18next**가 번역 엔진이라면,  **react-i18next**는 그 엔진을 React 컴포넌트에서 쉽게 다루게 해주는 연결자(adapter)입니다.  

---

## ⚙️ 설치 방법

`react-i18next`는 **i18next(번역 엔진)**을 React에서 쉽게 사용할 수 있도록 도와주는 라이브러리입니다.  
따라서 두 패키지를 함께 설치해야 합니다.

```bash
npm install i18next react-i18next
# 또는
yarn add i18next react-i18next
```

> 💡 **추가적으로**, 날짜, 숫자, 복수형 등 **언어별 포맷팅**이 필요한 경우에는  
> `i18next-icu` 플러그인을 함께 설치할 수 있습니다.  
> (이 부분은 다음 글 — *[i18next-icu로 포맷팅 자동화하기](https://velog.io/@sunmins/%EB%A6%AC%EC%95%A1%ED%8A%B8-%EB%9D%BC%EC%9D%B4%EB%B8%8C%EB%9F%AC%EB%A6%AC-%ED%84%B8%EA%B8%B0-1-i18next-icu%EB%A1%9C-%ED%8F%AC%EB%A7%B7%ED%8C%85-%EC%9E%90%EB%8F%99%ED%99%94%ED%95%98%EA%B8%B0)* — 에서 다룹니다.)

---

### 📁 설치 후 파일

기본적으로 설치만 하면 **별도의 파일은 자동으로 생성되지 않습니다.**  
대신 직접 **i18next 설정 파일(`i18n.ts` 또는 `src/i18n/index.ts`)**을 만들어야 합니다.

**예시 구조:**
```ts
src/
├─ i18n/
│ ├─ index.ts ← i18next 초기 설정 파일
│ ├─ en.ts ← 영어 번역 리소스
│ ├─ ko.ts ← 한국어 번역 리소스
└─ components/
└─ App.tsx
```

설정 파일(`i18n.ts`)에는 언어 리소스를 등록하고,  
`initReactI18next`를 통해 React와 연결하는 초기화 코드가 들어갑니다.

---

## 📄 설정 코드 핵심 예시 – src/i18n/index.ts

```ts
import i18n, { Resource } from "i18next";
import { initReactI18next } from "react-i18next";

// ✅ 번역 리소스 import
import { translation as en, menu as enMenu } from "./en";
import { translation as ko, menu as koMenu } from "./ko";

// ✅ 리소스 등록
const resources: Resource = {
  en: {
    translation: en,
    menu: enMenu,
  },
  ko: {
    translation: ko,
    menu: koMenu,
  },
} as const;

// ✅ 초기 설정
i18n
  .use(initReactI18next) // react-i18next와 연결
  .init({
    resources, // 언어 리소스 등록
    lng: sessionStorage.getItem("language") || "ko", // 초기 언어 설정
    fallbackLng: {
      en: ["ko"],
      default: ["ko"],
    },// fallbackLang:"ko"와 동일
    keySeparator: false, // 키에서 '.'을 구분자로 사용하지 않음
    interpolation: {
      escapeValue: false, // React는 XSS 방어가 내장되어 있으므로 false
    },
    react: {
      useSuspense: false, // 로딩 중 Suspense 비활성화
    },
  });

export default i18n;
```
- **`resources`** → 번역 리소스를 언어별로 등록  
- **`lng`** → 초기 언어 설정 (세션/스토리지 등에서 관리 가능)  
- **`fallbackLng`** → 번역 누락 시 기본 언어 지정  
- **`escapeValue: false`** → React의 XSS 방지 기능 중복 방지  
- **`.use(initReactI18next)`** → React와 i18next를 연결하는 핵심 단계  

---


**이 설정 파일을 한 번만 import**하면,  
프로젝트 전역에서 `useTranslation()` 훅으로 번역을 바로 사용할 수 있습니다.

```tsx
import "./i18n"; // App.tsx에서 한 번만 import

function App() {
  const { t } = useTranslation();
  return <h1>{t("menu.home")}</h1>;
}
```
---

## 📘 참고: `initReactI18next`란?

`initReactI18next`는 `react-i18next`에서 제공하는 **연결 어댑터(adapter)**입니다.  
`i18next` 번역 엔진을 **React 환경에서 사용할 수 있도록 바인딩**해주는 역할을 합니다.

즉,  
> `i18next`가 “번역 엔진”이라면,  
> `initReactI18next`는 그 엔진을 React 컴포넌트에 연결해주는 **“다리(bridge)”**입니다.

---

### 🔍 내부적으로 하는 일

1. **React Context 생성** → 번역 상태(`language`, `t` 함수 등)를 전역으로 관리  
2. **Provider 등록** → `I18nextProvider`를 자동 적용하여 모든 컴포넌트에서 접근 가능  
3. **React 훅 연결** → `useTranslation()` 훅으로 손쉽게 번역 함수 사용 가능  
---



# 💡 기본 개념 요약

```tsx
const { t, i18n } = useTranslation();
```

- **`t(key)`** : 현재 언어의 번역문을 반환하는 함수입니다.  
  예) `t("menu.home")` → `"홈"`  
  - 문자열 치환(**interpolation**)을 지원합니다.  
  - 복수형(`_plural`) 처리, 포맷팅 등 다양한 표현 기능을 제공합니다.  

- **`i18n`** : 전역 설정 객체입니다.  
  - 현재 언어(`i18n.language`)를 확인하거나,  
    언어 변경(`i18n.changeLanguage("en")`)을 담당합니다.  
  - “**i18n**”은 *internationalization(국제화)*의 약자이며,  
    “i”와 “n” 사이에 18개의 글자가 있어 **i18n**이라고 줄여 부릅니다. 

---

## 🧩 `t()` 함수의 주요 기능

###  1. 문자열 치환 (Interpolation)
> 번역문 안에 변수를 끼워 넣는 기능입니다.

**ko.json**
```json
{
  "greeting": "안녕하세요, {{name}}님!"
}
```

**React 코드**
```tsx
t("greeting", { name: "선민" }); // 안녕하세요, 선민님!
```

---

###  2. 복수형 처리 (Pluralization)
> 숫자(count)에 따라 문장을 다르게 표시하는 기능입니다.

**en.json**
```json
{
  "item": "{{count}} item",
  "item_plural": "{{count}} items"
}
```

**React 코드**
```tsx
t("item", { count: 1 }); // 1 item
t("item", { count: 5 }); // 5 items
```
👉 영어처럼 복수형이 있는 언어는 _plural 키를 자동으로 인식해서 변환합니다.
> ⚙️ 참고: 영어처럼 복수형이 있는 언어는 `_plural` 키를 자동으로 인식하지만,    한국어처럼 복수형 구분이 약한 언어는 단일 키만 써도 무방합니다.

---

### 🗓️ 3. 포맷팅 (Formatting)
> 숫자, 날짜 등의 값을 형식화하여 표시할 수 있습니다.  
> (고급 사용 시 `i18next-icu` 플러그인을 함께 사용합니다.)

**ko.json**
```json
{
  "price": "가격: {{value, number}}원",
  "today": "오늘은 {{date, datetime}} 입니다."
}
```

**React 코드**
```tsx
t("price", { value: 15000 }); // 가격: 15,000원
t("today", { date: new Date() }); // 오늘은 2025년 11월 3일 입니다.
```

---

- **`i18n`** : 전역 설정 객체입니다.  
  - `i18n.language` → 현재 언어를 확인합니다.  
  - `i18n.changeLanguage("en")` → 언어를 변경합니다.  
  - `i18n.dir()` → 언어 방향(RTL 지원 언어 등)을 반환합니다.  
  - `i18n`은 **Internationalization(국제화)**의 약자이며, 숫자 “18”은 *i*와 *n* 사이의 18글자를 의미합니다.  
    즉, “i18n”은 “internationalization”을 짧게 표현한 이름입니다.  
    이 객체는 번역 리소스, 현재 언어 상태, 언어 변경 로직 등을 관리하며,  
    `react-i18next`의 핵심 엔진인 **i18next 인스턴스**를 가리킵니다.

---

이 훅은 `react-i18next`에서 제공하며,  
i18next의 기능을 **React 컴포넌트에서 쉽게 사용할 수 있도록 해주는 래퍼**입니다.


---

## 🧩 사용 예시

```tsx
import { useTranslation } from "react-i18next";

export function Menu() {
  const { t, i18n } = useTranslation("menu");

  return (
    <>
      <h1>{t("home")}</h1>
      <p>{t("greeting", { name: "선민" })}</p>
		<button onClick={() => i18n.changeLanguage("en")}>EN</button>
		<button onClick={() => i18n.changeLanguage("ko")}>KO</button>
  );
}
```
``` ts
// React에서는 보통 버튼 클릭으로 언어 전환을 구현합니다.
const handleChangeLang = (lang: string) => {
  i18n.changeLanguage(lang);
};
```

---

## 📁 폴더 구조 예시

### 🧱 작은 프로젝트 구조 (단일 파일 구성)
작은 프로젝트에서는 다음과 같이 단일 파일로 구성할 수 있습니다.

```
src/
 ├─ i18n/
 │   ├─ en.ts
 │   ├─ ko.ts
 │   └─ index.ts
 └─ components/
     └─ Menu.tsx
```

### 🗂️ 중·대형 프로젝트 구조 (네임스페이스 기반)
하지만 **프로젝트 규모가 커지면 언어별 / 기능별로 파일을 나누는 것이 유지보수에 훨씬 유리합니다.**  
이때 사용하는 것이 바로 **네임스페이스(namespace)** 구조입니다.
```
src/
└─ i18n/
├─ locales/
│ ├─ en/
│ │ ├─ translation.json // 공통 텍스트
│ │ ├─ menu.json // 메뉴 관련
│ │ ├─ error.json // 에러 메시지
│ │ └─ profile.json // 프로필 화면
│ └─ ko/
│ ├─ translation.json
│ ├─ menu.json
│ ├─ error.json
│ └─ profile.json
└─ index.ts // i18next 초기 설정
```

### 🧩 사용 예시

```tsx
// menu.json 파일에 접근할 때
const { t } = useTranslation("menu");
console.log(t("home")); // 홈

// error.json 파일에 접근할 때
const { t: tError } = useTranslation("error");
console.log(tError("notFound")); // 페이지를 찾을 수 없습니다.
```

이렇게 하면, 파일 단위로 **UI 영역별 / 언어별 텍스트를 분리**할 수 있습니다.  
각 파일은 자동으로 **하나의 네임스페이스(namespace)** 로 인식됩니다.

**팁:**  
- 리소스 파일은 `translation`, `menu`, `error` 등 네임스페이스 단위로 나누면 관리가 편리합니다.  
- 프로젝트가 커질수록 `locales/en/`, `locales/ko/` 폴더 구조를 사용하는 것을 추천합니다.

---

## 📚 en.json / ko.json 구조 설계 가이드

i18next 리소스 파일은 **두 가지 스타일**로 작성할 수 있습니다.  
둘 다 지원하지만, **프로젝트 규모와 목적**에 따라 선택하는 것이 좋습니다.

---

### 1️⃣ 평(flat) 구조 (작은 프로젝트에 추천)

**en.json**
```json
{
  "home": "Home",
  "about": "About",
  "contact": "Contact"
}
```
ChatGPT의 말:
**장점**  
- 빠르게 작성할 수 있습니다.  
- 간단한 UI 텍스트 중심 프로젝트에 적합합니다.  

**단점**  
- 키 충돌 가능성이 있습니다.  
- 화면 단위로 구분하기 어렵습니다.  

### 2️⃣ 중첩(nested) 구조 (중대형 프로젝트에 추천)

**en.json**
```json
{
  "menu": {
    "home": "Home",
    "about": "About",
    "contact": "Contact"
  },
  "button": {
    "save": "Save",
    "cancel": "Cancel"
  }
}
```


**ko.json**
```json
{
  "menu": {
    "home": "홈",
    "about": "소개",
    "contact": "문의하기"
  },
  "button": {
    "save": "저장",
    "cancel": "취소"
  }
}
```

**장점**  
- 네임스페이스 없이도 UI 영역별로 구조화가 가능합니다.
- 협업 시 유지보수와 관리가 용이합니다.
- 키 충돌을 방지할 수 있습니다.

**단점**  
- 키 경로(menu.home) 입력이 다소 길어집니다.


## 🧠 자주 사용하는 i18n 기능

| 기능 | 코드 예시 | 설명 |
|------|------------|------|
| 변수 치환 | `t("welcome", { name: "민준" })` | 문자열 내 변수 바인딩 |
| 복수형 처리 | `t("item", { count: 5 })` | `item_plural` 키 자동 인식 |
| 언어 전환 | `i18n.### 1️⃣ 평(flat) 구조 (작은 프로젝트에 추천)("en")` | 실시간 언어 변경 |
| 현재 언어 확인 | `i18n.language` | 현재 활성 언어 코드 |
| 언어 방향 | `i18n.dir()` | LTR / RTL 여부 확인 |

---

## ✅ 정리
- `t()`는 **번역 함수**, `i18n`은 **국제화 설정 객체(i18next 인스턴스)**입니다.  
- `resources`에 언어별 JSON 파일을 등록해 사용합니다.  
- `i18n.changeLanguage("en")`으로 **언어를 실시간 전환**할 수 있습니다.  
- React에서는 `escapeValue: false` 설정으로 **XSS 방지를 중복 처리하지 않도록** 해야 합니다.  
- 리소스 구조는 **작은 프로젝트는 평(flat)**, **중대형 프로젝트는 중첩(nested)** 구조가 적합합니다.  
- 규모가 커질수록 **네임스페이스(namespace)** 로 파일을 분리하면 유지보수가 용이합니다.  
- ✅ **가장 추천되는 형태:**  
  > **“네임스페이스 구조 안에서 nested 구조를 사용하는 방식”**  
  > (파일 단위로 기능을 나누고, 각 파일 내부에서는 계층적으로 키를 관리)
